# Source

Mirrored from <https://github.com/earendil-works/pi> (`packages/coding-agent/docs` and `README.md`)
on 2026-08-25 by `docs/harnesses/pull.sh`.

Pi. cctop reads its sessions under `$PI_CODING_AGENT_DIR`, default `~/.pi/agent`.

Upstream's own licence applies to everything in this directory.
