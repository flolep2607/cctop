name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  # Shared with Release, which runs the same checks before it tags a version.
  verify:
    uses: ./.github/workflows/verify.yml
